node_modules/
.env*.local

# Scan output — regenerated by `deepsec scan` / `process`. INFO.md
# and SETUP.md (manually edited) stay tracked.
data/*/files/
data/*/runs/
data/*/reports/
data/*/project.json

# Exported findings — contain vulnerability detail (file paths, line ranges,
# exploit scenarios). Keep out of public history.
findings/
